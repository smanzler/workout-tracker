import { StyleSheet, Text, View } from "react-native";
import React from "react";

const WorkoutPicker = () => {
  return (
    <View>
      <Text>WorkoutPicker</Text>
    </View>
  );
};

export default WorkoutPicker;

const styles = StyleSheet.create({});
